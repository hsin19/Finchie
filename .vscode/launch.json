{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "data-pipeline(python): Current Module",
            "type": "debugpy",
            "request": "launch",
            "justMyCode": true,
            "module": "${fileBasenameNoExtension}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/services/data-pipeline",
            "env": {
                "PYTHONPATH": "${fileDirname}"
            }
        }
    ]
}